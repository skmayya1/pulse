<%# locals: (organization:) %>

<article class="card border border-base-300 bg-base-100">
  <div class="card-body gap-1 p-5">
    <p class="text-sm text-base-content/60">Organization</p>

    <h2 class="card-title text-xl"><%= organization.name %></h2>
  </div>
</article>
