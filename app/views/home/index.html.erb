<% content_for :title, "Pulse" %>

<main class="min-h-screen bg-base-100 px-6 py-12">
  <div class="mx-auto max-w-5xl">
    <p class="text-sm font-medium text-primary">Pulse</p>

    <h1 class="mt-2 text-3xl font-semibold text-base-content">
      Your organizations
    </h1>

    <div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      <%= render partial: "home/organization", collection: @organizations %>
    </div>
  </div>
</main>
