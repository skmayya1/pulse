<%# locals: (flash_messages:) %>

<% if flash_messages.present? %>
  <div class="toast toast-top toast-end z-50" aria-live="polite">
    <%= render partial: "application/flash_message", collection: flash_messages, as: :flash_message %>
  </div>
<% end %>
